# Docker secrets (the .env file next to docker-compose.yml).
# Don't fill this in by hand — run:  ./scripts/generate-secrets.sh
POSTGRES_PASSWORD=
APP_DB_USER=gharkhoji_app
APP_DB_PASSWORD=
REDIS_PASSWORD=
