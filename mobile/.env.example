# Copy to .env  (cp .env.example .env)  and put YOUR laptop's Wi-Fi IP address.
# Find it in Windows PowerShell with:  ipconfig   → "Wireless LAN adapter Wi-Fi" → IPv4 Address
EXPO_PUBLIC_API_URL=http://192.168.1.10:8000/api/v1
